# TODOS

仅保留当前仍未完成或只完成了一部分的事项。已完成、Phase 1 旧阶段说明、以及已被 Local-First 路线替代的待办不再保留在本文件。

## TODO-26: 护理规则交互反馈收口

**状态:** 部分完成。

**What:** 护理规则已改为 `BSheet` 表单和 `BDeleteConfirm` 删除确认，本地读写也已接入 local-first。仍需补齐操作反馈体验：

- 启用模板时提供明确 loading / disabled 状态，防止重复点击
- 新建、启用、删除后尽量做到即时列表反馈
- 失败时恢复可理解状态并 toast 提示

**验收:**

- 验证失败时表单保持打开
- 启用模板期间有明确反馈
- 删除确认使用 `BDeleteConfirm`
- 操作成功后列表立即反映结果，不等待额外网络全量刷新

## TODO-28: 支出分类管理乐观反馈收口

**状态:** 部分完成。

**What:** 支出分类管理已完成真实布局骨架屏、`BSheet` 新建/编辑、`BDeleteConfirm` 删除确认。仍需补齐乐观反馈：

- 新建/编辑分类或分组后，列表即时反映
- 删除分类或分组后，列表即时移除
- 失败时恢复原列表并 toast 提示

**验收:**

- 不出现操作成功后明显等待刷新才变化的体感
- local-first 本地事务仍是事实源，不回退到直接等云端结果
- 失败恢复不破坏本地缓存与 outbox 状态

## TODO-29: 首页工作台密度自适应优化

**状态:** 部分完成。

**What:** 已有 `buildHomeWorkbench`、固定四区块、繁育分组与繁育轻量 row/展开机制。仍需把密度自适应扩展到完整首页：

- 逾期：少量展开，多量展示前几条 + 查看更多
- 健康：按疫苗 / 驱虫 / 疾病观察聚合，保留真正适合批量处理的批量卡
- 用药：按未完成 / 部分完成 / 已完成组织，突出今日执行
- 区块计数、红点和可见卡片数保持一致

**验收:**

- 首页仍固定为 `逾期 / 繁育 / 健康 / 用药`
- 大量任务时不再全部使用大卡片直出
- 首页只处理 `home` scope，不预拉非首页数据
- 来源页提交后本地承接、防闪回、latest token 与 suppression 仍有效
