# 页面清单（Page Inventory）

> 基于设计文档 01~05 全量梳理，覆盖所有用户流程。
> 最后更新：2026-03-25

---

## 统计概览

| 类型 | 数量 |
|------|------|
| Page（全页面） | 41 |
| Sheet（底部弹出） | 26 |
| Modal（弹窗/确认框） | 25 |
| Step（向导步骤） | 3 |
| Sub-view（子视图/标签页） | 15 |
| **合计唯一屏幕** | **110** |

> **变更记录（2026-03-28）：** R-1 从 Page 改为 Sub-view（FAB Sheet 子视图），S-2 从 Sheet 改为 Page。S-6/S-7 HTML 原型为 Page 但实现为 BSheet（嵌入详情页，交互更优）。

> **关于 `dogs.species` 字段：** V1 新建犬只时 species 字段默认填入「犬」，不在表单中显示。未来支持猫品类时再添加选择入口。

---

## 一、认证流程（uni-id-pages 预置页面）

> 使用 uni-id-pages 插件提供的预置 UI，无需从零开发。Phase 1 实现。

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| A-1 | 登录页 | Page | 1 | 手机号+验证码登录、用户名密码登录、微信登录、Apple 登录（iOS 必须）| APP 启动未登录时 / Token 过期 | 首页 / 注册页 / 创建家庭页 |
| A-2 | 注册页 | Page | 1 | 手机号注册、设置密码 | 登录页「注册」/ 邀请链接 | 创建家庭页 / 加入家庭 |
| A-3 | 忘记密码页 | Page | 1 | 手机号验证 + 重置密码 | 登录页「忘记密码」 | 登录页 |
| A-4 | 创建/加入家庭页 | Page | 1 | 新用户首次登录：创建犬舍名称 或 通过邀请码加入已有家庭 | 注册成功后 | 首页 |

**空状态：** 登录页无特殊空状态
**错误状态：** 验证码错误、密码错误、网络错误、手机号已注册

---

## 二、首页 Tab（首页）

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| H-1 | 首页 | Page | 3 | 三层结构：Zone 1 状态摘要栏（需处理/今日/本周数字）+ 7 天预览条 + Zone 2 智能卡片区（逾期/今日/即将到来） | 底部导航「首页」 | 各卡片操作入口 |
| H-2 | 月历视图 | Sheet | 3 | 完整月历展开，显示每日事件数量，支持选择日期查看事项 | 首页 7 天预览条点击月份标题 | 返回首页（选中日期后下方卡片切换） |
| H-3 | 简单任务完成表单 | Sheet | 3 | 快速标记任务完成（如疫苗打完、药喂完），含日期、备注 | 首页卡片「记录结果」/「标记完成」按钮 | 返回首页（卡片消失/变灰） |
| H-4 | 推迟任务表单 | Sheet | 3 | 选择新提醒日期（默认明天）+ 可选填原因 | 逾期卡片「推迟」按钮 | 返回首页（卡片移出逾期区） |
| H-5 | 批量任务完成表单 | Sheet | 3 | 批量标记群组/批量卡片中的多只犬完成同一任务 | 群组卡片/批量卡片「批量标记今日已完成」 | 返回首页 |
| H-6 | 批量处理页 | Page | 3 | 犬只勾选列表（默认全选）、药品/疫苗选择、费用输入（自动均分）、日期、确认完成 | 批量卡片「批量处理全部」 | 返回首页 |

**空状态：** 「犬群一切正常 · 距下一个事项还有 X 天」
**错误状态：** 网络错误（显示缓存数据 + 刷新提示）
**加载状态：** 骨架屏（摘要栏 + 卡片占位）

### 首页卡片子视图（不独立路由，作为卡片组件）

| # | 名称 | 类型 | Phase | 关键内容 |
|---|------|------|-------|---------|
| H-S1 | 个体犬只卡片 | Sub-view | 3 | 犬名 + 状态天数 + 当日任务 + 操作按钮 |
| H-S2 | 状态群组卡片 | Sub-view | 3 | 群组名 + 成员列表 + 共同任务 + 批量完成按钮 |
| H-S3 | 批量卡片 | Sub-view | 3 | 事件类型 + 犬只数量 + 犬只列表摘要 + 批量处理/逐只查看按钮 |
| H-S4 | 连续用药卡片 | Sub-view | 3 | 进度（第 X 天/共 N 天）+ 犬只药品列表 + 批量标记按钮 |

---

## 三、档案 Tab（档案）

### 3.1 犬只列表与详情

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| D-1 | 犬只列表页 | Page | 1 | 犬只卡片列表、按性别/角色/disposition 筛选、搜索、排序；显示派生状态标签 | 底部导航「档案」 | 犬只详情 / 新建犬只 |
| D-2 | 犬只详情页 | Page | 1 | 基础信息、当前状态标签、繁育历史、健康记录、体重曲线、财务摘要（Phase 2）、窝列表（种母）、「+ 添加记录」浮动按钮、「标记状态」按钮 | 犬只列表点击 / 首页卡片点击犬名 / 各处犬名链接 | 编辑犬只 / 窝详情 / 添加记录 / 繁育周期详情 |
| D-3 | 新建犬只页 | Page | 1 | 表单：昵称（选填）、性别、角色（种狗/幼崽/外部种公）、品种、出生日期、购入价格（选填）、购入日期（选填）；外部种公：主人信息 | 犬只列表「+」按钮 / 快速记录 | 犬只详情页 |
| D-4 | 编辑犬只页 | Page | 1 | 同新建犬只表单，可修改已有字段 | 犬只详情页「编辑」 | 犬只详情页 |
| D-5 | 犬只筛选面板 | Sheet | 1 | 多维筛选：性别（公/母）、角色（种狗/幼崽/外部种公）、disposition（在养/待售/已预定等）、状态（发情中/怀孕中等） | 犬只列表「筛选」按钮 | 返回犬只列表（应用筛选） |

### 3.2 犬只详情 - 子视图（Tab 或 Section）

| # | 名称 | 类型 | Phase | 关键内容 |
|---|------|------|-------|---------|
| D-S1 | 基础信息区 | Sub-view | 1 | 昵称、性别、角色、品种、出生日期、年龄、体重、disposition、来源窝 |
| D-S2 | 繁育历史区 | Sub-view | 1 | 按周期展示繁育记录列表（发情 → 配种 → 孕检 → 生产），可展开看子记录 |
| D-S3 | 健康记录区 | Sub-view | 1 | 疫苗/驱虫/疾病/用药记录列表，按时间倒序 |
| D-S4 | 体重曲线区 | Sub-view | 1 | 体重历史曲线图 + 记录列表 |
| D-S5 | 财务摘要区 | Sub-view | 2 | 该犬相关收支汇总，点击跳转财务页筛选视图 |
| D-S6 | 窝列表区（仅种母） | Sub-view | 1 | 该种母所有窝的列表，含窝号/出生日期/幼崽数/存活数 |

### 3.3 犬只状态变更

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| D-6 | 快速标记状态 | Sheet | 1 | 选择状态类型（生病/其他）→ 选择病症类型（感冒/腹泻/皮肤病/其他）→ 确认 | 犬只详情页「标记状态」按钮 | 返回犬只详情页（状态标签更新） |
| D-7 | 标记退休确认 | Modal | 1 | 确认退休 + 退休日期 + 退休原因备注 | 犬只详情页（种狗 disposition 操作） | 返回犬只详情（disposition=已退休） |
| D-8 | 标记已故确认 | Modal | 1 | 确认死亡/夭折 + 死亡日期 + 死因备注 | 犬只详情页 disposition 操作 | 返回犬只详情（disposition=已故） |
| D-9 | 标记领养表单 | Sheet | 1 | 领养日期 + 领养说明备注 + 领养费（选填，有则录入收入） | 犬只详情页（退休犬/幼崽）disposition 操作 | 返回犬只详情（disposition=已领养） |
| D-10 | 标记赠送表单 | Sheet | 1 | 赠送日期 + 受赠人信息备注 | 犬只详情页 disposition 操作 | 返回犬只详情（disposition=已赠送） |
| D-11 | 取消退休确认 | Modal | 1 | 确认恢复在养状态 | 犬只详情页（已退休犬） | 返回犬只详情（disposition=在养） |
| D-12 | 幼崽升级为种狗确认 | Modal | 1 | 确认将幼崽（自留）升级为种狗，role 变更说明 | 犬只详情页（幼崽，disposition=自留） | 返回犬只详情（role=种狗，disposition=在养） |
| D-13 | 标记康复确认 | Modal | 1 | 确认疾病康复 + 康复日期 | 犬只详情页（生病中状态标签） | 返回犬只详情（退出生病中状态） |
| D-14 | 删除犬只确认 | Modal | 1 | 确认软删除，提示 30 天可恢复 | 犬只详情页「删除」 | 返回犬只列表 |

### 3.4 窝详情

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| D-15 | 窝详情页 | Page | 1 | 窝基本信息（种公、种母、生产日期、生产方式、经验心得）、幼崽列表（状态/去向/体重）、窝利润统计（Phase 2）、「记录体重」按钮、「+ 添加幼崽」按钮、「确认断奶」按钮 | 犬只详情繁育历史中点击窝 / 犬只详情窝列表 | 幼崽详情 / 批量体重录入 |
| D-16 | 添加幼崽表单 | Sheet | 1 | 性别、体重（选填）、存活状态、标识/昵称 | 窝详情页「+ 添加幼崽」 | 返回窝详情（幼崽列表更新） |
| D-17 | 确认断奶 | Modal | 1 | 确认断奶日期（默认今天） | 窝详情页「确认断奶」按钮（幼崽到达断奶龄时提示） | 返回窝详情（weaned_at 填入） |

### 3.5 繁育周期详情

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| D-18 | 繁育周期详情页 | Page | 1 | 周期状态流转图、种母/种公信息、所有子记录时间线（发情/卵泡/配种/孕检/产检/临产/生产/异常终止）、周期关联费用、关闭/放弃周期操作 | 犬只详情繁育历史点击周期 | 各子记录详情 / 添加子记录 / 窝详情 |
| D-22 | 关闭发情确认 | Modal | 1 | "确认手动关闭该发情记录？关闭后状态将回归正常，对应繁育周期将标记为放弃。" 可选填关闭原因 | D-18 繁育周期详情页（当周期处于发情中状态时显示"关闭发情"操作） | 关闭后返回犬只详情 |

### 3.6 体重管理

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| D-19 | 批量体重录入页 | Page | 4 | 选择窝 → 日期 → 逐只输入体重 → 显示上次体重和增减（下降标黄） → 保存全部 | 窝详情页「记录体重」/ 快速记录 | 返回窝详情 / 快速记录 |
| D-20 | 单犬体重录入 | Sheet | 1 | 体重数值输入 + 日期 | 犬只详情页体重区「+」 | 返回犬只详情（体重更新） |
| D-21 | 体重曲线详情 | Sheet | 1 | 某只犬的体重历史曲线图（全屏或大面板） | 犬只详情体重区点击图表 / 批量体重页点击某只幼崽 | 返回 |

**空状态（犬只列表）：** 「还没有犬只档案，点击 + 创建第一只」
**空状态（繁育历史）：** 「暂无繁育记录」
**空状态（健康记录）：** 「暂无健康记录」
**空状态（体重）：** 「暂无体重记录，记录第一次体重」
**错误状态：** 网络错误提示 + 重试；协助者访问受限功能时显示「无权限」提示

---

## 四、快速记录 Tab（快速记录）

### 4.1 FAB Action Sheet 与入口页

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| R-0 | FAB Action Sheet | Sheet | 1 | 智能推荐三槽位（待办 + 建议 + 常用，无匹配时 fallback 到最近操作或默认：支出/收入/体重）+ 底部「全部记录类型 →」链接 | 底部导航中间 "+" FAB 点击 | 各记录表单 / R-1 全部记录类型页 |
| R-1 | 全部记录类型（FAB Sheet 子视图） | Sub-view | 1 | 15 种记录类型图标网格：繁育记录（8 种）、健康记录（4 种）、财务记录（2 种）、体重记录。**注意：不是独立页面，是 R-0 FAB Sheet 内的第二视图，点击返回切回推荐视图** | FAB Action Sheet「全部记录类型 →」 | 各记录表单 |

### 4.2 繁育记录表单

> **实现说明（2026-03-28）：** 每种繁育记录类型为独立页面（breeding-heat.vue、breeding-follicle.vue 等），不再是 breeding.vue 内的 type 切换。所有表单包含 BFormOptions 公共组件（"标记为待办"开关 + 日期选择含今天/明天/后天 chips + "下次提醒"开关）。

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| R-2 | 发情记录表单 | Page | 1 | 独立页面 breeding-heat.vue。选择种母、发情开始日期、备注、费用（选填）。无进行中周期时自动创建 breeding_cycle | 快速记录 / 犬只详情「+ 添加记录」 | 返回来源页 |
| R-3 | 卵泡检查表单 | Page | 1 | 独立页面 breeding-follicle.vue。选择种母（自动关联当前周期）、日期、左右侧卵泡数量和大小、检查结果、费用（选填） | 快速记录 / 犬只详情 / 首页卡片 | 返回来源页 |
| R-4 | 配种记录表单 | Page | 1 | 独立页面 breeding-mating.vue。选择种母、选择种公（含外部种公）、配种方式、日期、第几次配种、费用（选填）；自动计算预计孕检日和预产期（可手动修改） | 快速记录 / 犬只详情 / 首页卡片 | 返回来源页 |
| R-5 | 孕检记录表单 | Page | 1 | 独立页面 breeding-pregnancy.vue。选择种母（自动关联怀孕中周期）、确认怀孕（是/否）、幼崽数量（选填）、B 超图片上传、费用（选填）；确认未怀孕 → 周期状态变为失败 | 快速记录 / 犬只详情 / 首页卡片 | 返回来源页 |
| R-6 | 产检记录表单 | Page | 1 | 独立页面 breeding-prenatal.vue。选择种母、日期、检查结果、费用（选填） | 快速记录 / 犬只详情 | 返回来源页 |
| R-7 | 临产监测表单 | Page | 1 | 独立页面 breeding-prelabor.vue。选择种母、体温（选填，低于阈值触发额外提醒）、是否刨窝、食欲变化、其他征兆 | 快速记录 / 犬只详情 / 首页卡片 | 返回来源页 |
| R-8 | 生产记录向导 | Page | 1 | 三步骤向导（详见下方 Step） | 快速记录 / 犬只详情 / 首页卡片 | 窝详情页 |
| R-9 | 异常终止妊娠表单 | Page | 1 | 独立页面 breeding-termination.vue。选择种母、终止类型（流产/死胎/医疗终止/确认未怀孕）、日期、备注、费用（选填）；周期状态 → 失败 | 快速记录 / 犬只详情 / 繁育周期详情 | 返回来源页 |

### 4.3 生产记录向导（三步骤）

| # | 步骤名称 | 类型 | Phase | 关键内容 |
|---|---------|------|-------|---------|
| R-8a | 步骤 1：母犬与生产信息 | Step | 1 | 母犬信息（自动填充）、生产日期、生产方式（顺产/难产/剖腹产）、费用（选填） |
| R-8b | 步骤 2：逐只录入幼崽 | Step | 1 | 每只幼崽：性别、体重（选填）、存活状态、标识/昵称；「+ 添加下一只」按钮；支持分次添加 |
| R-8c | 步骤 3：确认与心得 | Step | 1 | 确认创建窝 + 幼崽汇总 + 经验心得（选填）+ 完成按钮 |

### 4.4 健康记录表单

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| R-10 | 疫苗记录表单 | Page | 1 | 独立页面 health-vaccination.vue。BDogPicker 多选模式、疫苗类型、日期、费用（选填，多选时自动均分）、BFormOptions（待办/提醒开关） | 快速记录 / 犬只详情 / 首页卡片 / 批量处理页 | 返回来源页 |
| R-11 | 驱虫记录表单 | Page | 1 | 独立页面 health-deworming.vue。BDogPicker 多选模式、驱虫药品名、日期、费用（选填，多选时自动均分）、BFormOptions（待办/提醒开关） | 快速记录 / 犬只详情 / 首页卡片 / 批量处理页 | 返回来源页 |
| R-12 | 疾病记录表单 | Page | 1 | 独立页面 health-illness.vue。BDogPicker 多选模式、病症（感冒/腹泻/皮肤病/犬瘟/细小等）、严重程度（选填）、开始日期、治疗状态、备注、费用（选填，多选时自动均分）、BFormOptions | 快速记录 / 犬只详情 / 补录提醒（快速标记后） | 返回来源页 |
| R-13 | 用药记录表单 | Page | 1 | 选择犬只（可多选）、药品名、剂量+单位、给药方式、频率、持续天数、开始日期、备注；提供「从方案库选择」入口；完成后提示「保存为方案？」 | 快速记录 / 犬只详情 / 首页卡片 | 返回来源页 |
| R-14 | 从方案库选择 | Sheet | 4 | 方案库列表、选择方案后自动填充表单字段、按体重自动换算剂量 | 用药记录表单「从方案库选择」 | 返回用药记录表单（字段已填充） |
| R-15 | 保存为方案确认 | Modal | 4 | 方案名称、适用病症、适用体重范围、确认保存 | 用药记录完成后弹出提示 | 关闭 |

### 4.5 财务记录（快速记录入口）

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| R-16 | 支出录入表单 | Page | 2 | 极简表单：金额、分类（显示为单行 tag，点击打开分类选择 Sheet）、日期、关联（显示为文字链接，点击打开关联选择 Sheet，四选一：繁育周期/窝/犬只/无）、图片凭证（选填）、备注 | 快速记录 / 财务页「+」 | 返回来源页 |
| R-17 | 收入录入表单 | Page | 2 | 收入类型（销售/定金保留/领养/其他）、金额、关联犬只（选填）、日期、备注 | 快速记录 / 财务页「+」 | 返回来源页 |
| R-16a | 分类选择 Sheet | Sheet | 2 | 支出分类列表（预设+自定义），选择后以单行 tag 显示在表单中 | R-16 支出表单点击分类 tag | 返回 R-16（分类已选） |
| R-16b | 关联选择 Sheet | Sheet | 2 | 四种关联方式选择：繁育周期/窝/犬只/无关联。选择后根据类型打开对应的选择器（G-1 犬只选择器 / G-2 窝选择器 / G-3 繁育周期选择器） | R-16 支出表单点击关联文字链接 | 返回 R-16（关联已选） |

### 4.6 体重记录（快速记录入口）

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| R-18 | 体重记录入口 | Sheet | 1 | 选择：单犬录入 / 批量录入（选窝） | 快速记录「体重记录」 | 单犬体重录入(D-20) / 批量体重录入(D-19) |

### 4.7 发情观察日志（快速监测日志模式）

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| R-19 | 发情观察日志 | Page | 1 | 快速监测日志模式：dog-select-card 智能预填当前发情中的犬、外阴状态（硬/肿胀、开始软化、明显松软）、分泌物状态（鲜红较多、暗红减少、淡粉/草黄色、接近透明）、行为征兆多选（主动靠近公犬、接受爬跨、翘尾侧偏、频繁排尿、舔舐外阴增多）、备注 | 快速记录 / 犬只详情 / 首页发情周期弱入口（当前实现为「建议卵泡检查」） | 返回来源页 |

> **R-19 vs R-2 的区别：** R-2 是「录入发情记录」（首次记录发情开始），R-19 是「发情观察日志」（发情期间每日记录外阴/分泌物/行为变化，用于判断最佳配种时机）。

### 4.8 记录详情与编辑

#### 繁育记录详情/编辑（8种）

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| R-20 | 繁育记录详情页 | Page | 1 | 展示单条繁育子记录的完整信息（根据 type 展示对应 details 字段） | D-18 繁育周期详情页点击某条记录 | 返回周期详情 / 进入编辑 |
| R-21 | 繁育记录编辑页 | Page | 1 | 编辑已有繁育子记录（与创建表单字段相同，预填已有数据）。配种记录修改日期时自动重算预产期（如非手动覆盖）并更新关联 tasks | R-20 详情页点击编辑 | 保存后返回详情页 |

#### 健康记录详情/编辑（3种）

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| R-22 | 健康记录详情页 | Page | 1 | 展示单条健康记录完整信息（疫苗类型/驱虫药品/疾病详情等） | D-S3 健康记录区点击某条 | 返回犬只详情 / 进入编辑 |
| R-23 | 健康记录编辑页 | Page | 1 | 编辑已有健康记录 | R-22 点击编辑 | 保存后返回详情页 |

#### 用药任务详情/编辑/取消

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| R-24 | 用药任务详情页 | Page | 1 | 展示用药任务完整信息（药品、剂量、频率、进度第X天/共N天、每天完成记录列表） | 首页用药卡片点击 / D-S3 健康记录区 | 返回 |
| R-25 | 用药任务编辑页 | Page | 1 | 编辑用药任务（可修改剂量、持续天数等） | R-24 点击编辑 | 保存返回 |
| R-26 | 取消用药确认 | Modal | 1 | "确认取消该用药任务？取消后不可恢复，已完成的天数记录保留。" | R-24 详情页操作菜单 | 关闭 |

---

## 五、财务 Tab（财务）

### 5.1 财务主页与流水

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| F-1 | 财务主页 | Page | 2 | 月度收支汇总卡片 + 统一流水列表（收入红色/支出绿色，中式记账配色，按时间排序）+ 筛选按钮 + 「+」录入按钮 | 底部导航「财务」 | 支出/收入详情 / 统计视图 / 支出录入 |
| F-2 | 流水筛选面板 | Sheet | 2 | 按时间范围、按分类、按犬只、按窝筛选 | 财务主页「筛选」按钮 | 返回财务主页（应用筛选） |
| F-3 | 支出详情页 | Page | 2 | 支出全部字段展示、关联犬只/周期/窝信息、图片凭证、来源标记（手动/自动）；自动记录只读 | 财务流水列表点击支出项 | 编辑支出 / 返回 |
| F-4 | 收入详情页 | Page | 2 | 收入全部字段展示、关联犬只/销售记录信息 | 财务流水列表点击收入项 | 返回 |
| F-5 | 编辑支出页 | Page | 2 | 同支出录入表单，编辑已有记录；source_type=auto 的不可编辑 | 支出详情页「编辑」 | 返回支出详情 |
| F-10 | 编辑收入页 | Page | 2 | 编辑手动录入的收入记录（自动生成的 source_type=auto 不可编辑，显示"此记录由系统自动生成，请在来源记录中修改"）。字段同 R-17 收入录入表单 | F-4 收入详情页点击编辑 | 保存返回 |

### 5.2 统计视图

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| F-6 | 统计总览页 | Page | 2 | 月度/年度切换、总收入/总支出/净利润、分类支出占比图 | 财务主页「统计」入口 | 单窝利润 / 种母 ROI |
| F-S1 | 月度/年度总账视图 | Sub-view | 2 | 月度净利润 = sum(incomes) - sum(expenses)、趋势图 |
| F-S2 | 分类支出视图 | Sub-view | 2 | 各分类支出占比饼图/柱状图 |
| F-7 | 单窝利润详情页 | Page | 2 | 窝收入（各幼崽销售收入 + 预计收入）- 窝支出（繁育过程+窝级别+幼崽个体费用）= 净利润；每只幼崽均摊成本 | 统计页 / 窝详情页 | 返回 |
| F-8 | 种母投资回报页 | Page | 2 | 种母购入价 + 所有窝总支出 + 个体费用 vs 所有窝总收入 = 累计净收益；各窝利润列表 | 统计页 / 犬只详情财务摘要 | 各窝利润详情 |
| F-9 | 未来预估页 | Page | 2 | 基于历史数据的收入预估：在役种母数、平均窝间隔、年轻犬到达繁育年龄预测；用户可修改预估参数 | 统计页（数据积累后开启） | 返回 |

**空状态（财务主页）：** 「暂无收支记录，点击 + 开始记账」
**空状态（统计）：** 「数据不足，至少需要 X 条记录才能生成统计」
**协助者权限：** 财务 Tab 整体不可见

---

## 六、销售流程（从档案 Tab 进入）

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| S-1 | 销售记录列表页 | Page | 2 | 所有销售记录列表，按状态分组（待售/已预定/已成交/已退款/定金取消），支持筛选 | 犬只详情 / 窝详情 / 「我的」页面 | 销售详情 |
| S-2 | 设定底价（待售） | Page | 2 | 底价输入 + 买家信息 + 备注，幼崽 disposition → 待售。**HTML 原型为 Page（pages-sales-flow.html）** | 犬只详情页（幼崽在养 → 标记待售）/ 快速记录 | 返回犬只详情 |
| S-3 | 预定表单（已预定） | Page | 2 | 定金金额（必填）、定金日期、买家信息（选填）、成交价（选填）、到手价（选填）、卖出人/代理人（选填，从代理人列表选择）、平台（预设+自定义）、notes（备注，选填） | 销售详情页「标记已预定」/ 犬只详情 | 返回销售详情 |
| S-4 | 完成交易表单（已成交） | Page | 2 | 已预定信息带入（可修改）、到手价（必填，低于底价标红提示）、交付日期、notes（备注，选填）、确认 → 自动生成收入记录 | 销售详情页「标记已成交」 | 返回销售详情 |
| S-5 | 销售详情页 | Page | 2 | 销售全流程信息、当前阶段、操作按钮（推进阶段/退款/定金取消） | 销售记录列表 / 犬只详情 | 返回 |
| S-6 | 退款表单 | Sheet | 2 | 退款金额（全额/部分）、退款原因（选填）、退款日期；全额退款 → 幼崽回到待售/在养。**HTML 原型为 Page（pages-remaining.html），实现为 BSheet 嵌入 S-5 详情页内** | 销售详情页「退款」 | 返回销售详情（状态=已退款）|
| S-7 | 定金取消表单 | Sheet | 2 | 定金处理方式（不退/退还/部分退还）、保留金额 → 如有保留计为收入；幼崽回到待售。**HTML 原型为 Page（pages-remaining.html），实现为 BSheet 嵌入 S-5 详情页内** | 销售详情页「定金取消」 | 返回销售详情（状态=定金取消）|
| S-8 | 选择平台 | Sheet | 2 | 预设平台列表（线下/微信/小红书/抖音/快手/闲鱼）+ 自定义添加 | 预定/成交表单中「平台」字段 | 返回表单 |
| S-9 | 选择代理人 | Sheet | 2 | 代理人列表 + 「+ 新增代理人」入口 | 预定/成交表单中「卖出人」字段 | 返回表单 |
| S-10 | 到手价低于底价提示 | Modal | 2 | 红色警告「到手价低于底价」，确认继续或修改 | 完成交易表单提交时 | 返回表单 / 继续提交 |

**协助者权限：** 销售相关页面全部不可见

---

## 七、我的 Tab（我的）

### 7.1 我的主页

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| M-1 | 我的主页 | Page | 1 | 个人信息、家庭/犬舍名称、功能入口列表：家庭管理、通知设置、用药方案库、合作代理人、支出分类管理、数据备份/导出、关于 | 底部导航「我的」 | 各设置子页 |

### 7.2 家庭与协作管理

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| M-2 | 家庭管理页 | Page | 4 | 犬舍名称编辑、成员列表（角色/状态）、邀请链接生成、移除成员、角色调整 | 我的主页「家庭管理」 | 邀请页 / 成员详情 |
| M-3 | 生成邀请链接 | Sheet | 4 | 生成邀请链接/二维码，默认角色：协助者 | 家庭管理页「邀请成员」 | 分享链接 |
| M-4 | 成员角色调整 | Sheet | 4 | 选择角色（管理员/协助者），仅创建者/管理员可操作 | 家庭管理页成员列表长按/编辑 | 返回家庭管理 |
| M-5 | 移除成员确认 | Modal | 4 | 确认移除，提示历史操作记录保留 | 家庭管理页 | 返回家庭管理 |
| M-23 | 操作日志页 | Page | 4 | 展示家庭内所有成员的操作记录（谁在什么时间做了什么操作），支持按时间/成员/操作类型筛选 | M-2 家庭管理页 | 返回 |

### 7.3 通知设置

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| M-6 | 通知设置页 | Page | 3 | 推送总开关、各类推送开关（繁育/健康/财务/护理）、晨间摘要时间设置 | 我的主页「通知设置」 | 返回我的主页 |

### 7.4 用药方案库

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| M-7 | 用药方案库列表页 | Page | 4 | 方案列表（名称/适用病症/药品数量）、搜索、「+ 新建方案」 | 我的主页「用药方案库」/ 用药记录表单「从方案库选择」 | 方案详情 / 新建方案 |
| M-8 | 用药方案详情/编辑页 | Page | 4 | 方案名称、适用病症、适用体重范围、药品列表（药名/剂量/给药方式/频率）、持续天数、注意事项 | 方案库列表点击 | 返回列表 |
| M-9 | 新建用药方案页 | Page | 4 | 同编辑页表单，新建 | 方案库列表「+」/ 用药记录后「保存为方案」 | 返回列表 |

### 7.5 代理人管理

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| M-10 | 代理人列表页 | Page | 4 | 代理人列表（名称/联系方式）、「+ 新增」 | 我的主页「合作代理人」/ 销售表单「选择代理人」 | 代理人详情 |
| M-11 | 代理人详情页 | Page | 4 | 代理人信息、经手中的交易列表、待收款汇总、历史已完成交易 | 代理人列表点击 | 编辑 / 返回 |
| M-12 | 新增/编辑代理人 | Sheet | 4 | 姓名、联系方式（选填） | 代理人列表「+」/ 代理人详情「编辑」/ 销售表单内「+ 新增代理人」 | 返回列表/表单 |

### 7.6 支出分类管理

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| M-13 | 支出分类管理页 | Page | 2 | 分类列表（食品/营养品/消耗品/日常用品/固定开销/交通/医疗/配种费/其他）、拖拽排序、新增/编辑/删除自定义分类 | 我的主页「支出分类管理」 | 返回我的主页 |
| M-14 | 新增/编辑分类 | Sheet | 2 | 分类名称输入 | 分类管理页「+ 新增」/ 编辑 | 返回分类管理 |

### 7.7 护理规则管理

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| M-15 | 护理规则列表页 | Page | 3 | 规则列表（触发状态/任务描述/频率），首次使用提供常见模板（孕期喂钙铁等） | 我的主页「护理规则」 | 新增/编辑规则 |
| M-16 | 新增/编辑护理规则 | Sheet | 3 | 触发状态选择（怀孕中/哺乳中/生病中/用药中）、任务描述、频率（每日/每周/自定义） | 护理规则列表「+」/ 编辑 | 返回列表 |

### 7.8 默认参数设置

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| M-17 | 默认参数设置页 | Page | 1 | 默认断奶龄（天）、默认疫苗间隔（天）、幼犬驱虫间隔（天）、成犬驱虫间隔（天） | 我的主页「默认参数」 | 返回我的主页 |

### 7.9 数据管理

| # | 页面名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|---------|------|-------|---------|------|------|
| M-18 | 数据备份/导出页 | Page | 1 | 上次备份时间、手动备份按钮、自动备份设置（每周）、导出全量 JSON 到本地设备、回收站入口 | 我的主页「数据备份/导出」 | 回收站 |
| M-19 | 回收站页 | Page | 1 | 30 天内软删除的犬只列表，可恢复或永久删除 | 数据备份页「回收站」 | 返回 |
| M-20 | 恢复犬只确认 | Modal | 1 | 确认恢复软删除的犬只 | 回收站列表项操作 | 返回回收站 |
| M-21 | 永久删除确认 | Modal | 1 | 警告不可恢复，确认永久删除 | 回收站列表项操作 | 返回回收站 |
| M-22 | 导出进度 | Modal | 1 | 导出进度条 + 完成后下载/分享 | 数据备份页「导出」按钮 | 返回 |

---

## 七-b、通用确认弹窗（删除/取消操作）

| # | 名称 | 类型 | Phase | 关键内容 | 入口 | 出口 |
|---|------|------|-------|---------|------|------|
| GM-1 | 删除繁育记录确认 | Modal | 1 | "确认删除该繁育记录？关联的费用记录也将删除。" | R-20 详情页操作菜单 | 关闭 |
| GM-2 | 删除健康记录确认 | Modal | 1 | "确认删除该健康记录？关联的提醒任务也将取消。" | R-22 详情页操作菜单 | 关闭 |
| GM-3 | 删除费用记录确认 | Modal | 2 | "确认删除该费用记录？" | F-3 支出详情页 | 关闭 |
| GM-4 | 删除收入记录确认 | Modal | 2 | "确认删除该收入记录？仅可删除手动录入的收入，自动生成的收入需从来源操作。" | F-4 收入详情页 | 关闭 |
| GM-5 | 删除销售记录确认 | Modal | 2 | "确认删除该销售记录？" | S-5 销售详情页 | 关闭 |

---

## 八、全局通用组件（非独立页面，跨页面复用）

| # | 名称 | 类型 | 说明 |
|---|------|------|------|
| G-1 | 犬只选择器（DogPicker） | Sheet | 单选/多选犬只，支持搜索 + 最近使用 + 按角色筛选（种狗/幼崽/外部种公） |
| G-2 | 窝选择器 | Sheet | 选择窝（按种母分组），支持搜索 |
| G-3 | 繁育周期选择器 | Sheet | 选择关联的繁育周期，支持搜索 |
| G-4 | 日期选择器 | Sheet | 日期选择（dayjs 格式化显示） |
| G-5 | 图片上传组件 | Sub-view | 拍照/相册选择 + 压缩上传 |
| G-6 | 网络错误提示 | Modal | 统一的网络错误弹窗 + 重试按钮 |
| G-7 | 权限不足提示 | Modal | 协助者访问受限功能时的提示 |
| G-8 | 操作成功反馈 | Modal | 轻量 Toast 或底部提示条 |

---

## 九、按 Phase 分期汇总

### Phase 1（犬只档案 + 繁育流程 + 基础提醒）

| 类型 | 页面编号 |
|------|---------|
| 认证 | A-1, A-2, A-3, A-4 |
| 档案 | D-1 ~ D-22（除 D-19 为 Phase 4） |
| 快速记录 | R-0, R-1 ~ R-13, R-18, R-19, R-20 ~ R-26 |
| 通用弹窗 | GM-1, GM-2 |
| 我的 | M-1, M-17, M-18, M-19, M-20, M-21, M-22 |
| **小计** | 约 57 个屏幕 |

### Phase 2（财务模块 + 销售流程）

| 类型 | 页面编号 |
|------|---------|
| 财务 | F-1 ~ F-9, F-S1, F-S2 |
| 销售 | S-1 ~ S-10 |
| 快速记录（财务） | R-16, R-16a, R-16b, R-17 |
| 财务编辑 | F-10 |
| 通用弹窗 | GM-3, GM-4, GM-5 |
| 我的（分类管理） | M-13, M-14 |
| 犬只详情（财务摘要子视图） | D-S5 |
| **小计** | 约 31 个屏幕 |

### Phase 3（首页智能卡片 + 自动化逻辑）

| 类型 | 页面编号 |
|------|---------|
| 首页 | H-1 ~ H-6, H-S1 ~ H-S4 |
| 通知设置 | M-6 |
| 护理规则 | M-15, M-16 |
| **小计** | 约 13 个屏幕 |

### Phase 4（协作 + 用药方案库 + 代理人 + 批量体重）

| 类型 | 页面编号 |
|------|---------|
| 家庭协作 | M-2 ~ M-5, M-23 |
| 用药方案库 | M-7 ~ M-9, R-14, R-15 |
| 代理人 | M-10 ~ M-12 |
| 批量体重 | D-19 |
| **小计** | 约 13 个屏幕 |

---

## 十、状态清单（空状态 / 错误状态 / 加载状态）

### 空状态

| 页面 | 空状态文案 |
|------|----------|
| 首页（无任务） | 「犬群一切正常 · 距下一个事项还有 X 天」 |
| 犬只列表（无犬只） | 「还没有犬只档案，点击 + 创建第一只」 |
| 犬只繁育历史（无记录） | 「暂无繁育记录」 |
| 犬只健康记录（无记录） | 「暂无健康记录」 |
| 犬只体重记录（无记录） | 「暂无体重记录，记录第一次体重」 |
| 财务流水（无记录） | 「暂无收支记录，点击 + 开始记账」 |
| 财务统计（数据不足） | 「数据不足，至少需要 X 条记录才能生成统计」 |
| 销售记录列表（无记录） | 「暂无销售记录」 |
| 用药方案库（无方案） | 「暂无用药方案，创建第一个方案或从用药记录保存」 |
| 代理人列表（无代理人） | 「暂无合作代理人」 |
| 回收站（无数据） | 「回收站为空」 |
| 家庭成员列表（仅自己） | 「目前只有你一人，邀请家人一起管理」 |
| 护理规则列表（无规则） | 「暂无护理规则，使用常见模板快速创建」 |

### 错误状态

| 场景 | 处理方式 |
|------|---------|
| 网络错误（全局） | 统一弹窗提示 + 重试按钮；首页显示缓存数据 |
| Token 过期 | 自动跳转登录页 |
| 协助者访问受限功能 | 底部导航隐藏财务 Tab；相关入口不显示或点击提示「无权限，请联系管理员」 |
| 数据冲突（离线同步） | Last Write Wins + 时间戳，冲突时保留最新 |
| 写入失败 | 本地队列保留，后台重试 + 用户提示 |

### 加载状态

| 页面 | 加载方式 |
|------|---------|
| 首页 | 骨架屏（摘要栏占位 + 卡片占位） |
| 犬只列表 | 列表骨架屏 |
| 犬只详情 | 各区域分段加载骨架屏 |
| 财务流水 | 列表骨架屏 |
| 统计图表 | 图表占位 + 加载动画 |
| 表单提交 | 按钮 loading 状态 + 防重复提交 |

---

## 十一、页面路由参考（pages.json 全页面路由）

> 仅 Page 类型需要在 pages.json 中注册路由。Sheet/Modal/Step 为组件层面实现。

```
pages/home/index                    # H-1 首页
pages/dog/list                      # D-1 犬只列表
pages/dog/detail                    # D-2 犬只详情
pages/dog/create                    # D-3 新建犬只
pages/dog/edit                      # D-4 编辑犬只
pages/litter/detail                 # D-15 窝详情
pages/breeding/cycle-detail         # D-18 繁育周期详情
pages/weight/batch                  # D-19 批量体重录入
# pages/record/index              # R-1 已改为 FAB Sheet 子视图，不再需要独立路由
pages/record/heat                   # R-2 发情记录
pages/record/follicle              # R-3 卵泡检查
pages/record/mating                # R-4 配种记录
pages/record/pregnancy-check       # R-5 孕检记录
pages/record/prenatal-check        # R-6 产检记录
pages/record/pre-labor             # R-7 临产监测
pages/record/birth                 # R-8 生产记录向导
pages/record/abnormal-termination  # R-9 异常终止
pages/record/vaccination           # R-10 疫苗记录
pages/record/deworming             # R-11 驱虫记录
pages/record/illness               # R-12 疾病记录
pages/record/medication            # R-13 用药记录
pages/record/heat-observation       # R-19 发情观察日志
pages/record/expense               # R-16 支出录入
pages/record/income                # R-17 收入录入
pages/record/breeding-detail       # R-20 繁育记录详情
pages/record/breeding-edit         # R-21 繁育记录编辑
pages/record/health-detail         # R-22 健康记录详情
pages/record/health-edit           # R-23 健康记录编辑
pages/record/medication-task-detail # R-24 用药任务详情
pages/record/medication-task-edit  # R-25 用药任务编辑
pages/finance/index                # F-1 财务主页
pages/finance/expense-detail       # F-3 支出详情
pages/finance/income-detail        # F-4 收入详情
pages/finance/expense-edit         # F-5 编辑支出
pages/finance/income-edit          # F-10 编辑收入
pages/finance/statistics           # F-6 统计总览
pages/finance/litter-profit        # F-7 单窝利润
pages/finance/dam-roi              # F-8 种母ROI
pages/finance/projection           # F-9 未来预估
pages/sale/list                    # S-1 销售记录列表
pages/sale/reservation             # S-3 预定表单
pages/sale/completion              # S-4 完成交易
pages/sale/detail                  # S-5 销售详情
pages/batch/process                # H-6 批量处理
pages/profile/index                # M-1 我的主页
pages/profile/family               # M-2 家庭管理
pages/profile/operation-log        # M-23 操作日志
pages/profile/notifications        # M-6 通知设置
pages/profile/medication-protocols # M-7 用药方案库列表
pages/profile/protocol-detail      # M-8 方案详情/编辑
pages/profile/protocol-create      # M-9 新建方案
pages/profile/agents               # M-10 代理人列表
pages/profile/agent-detail         # M-11 代理人详情
pages/profile/expense-categories   # M-13 支出分类管理
pages/profile/care-rules           # M-15 护理规则列表
pages/profile/default-settings     # M-17 默认参数设置
pages/profile/backup               # M-18 数据备份/导出
pages/profile/recycle-bin          # M-19 回收站
uni_modules/uni-id-pages/...       # A-1~A-3 认证页面（插件预置）
pages/auth/create-family           # A-4 创建/加入家庭
```

**全页面路由总计：57 个**（含 uni-id-pages 预置 3 个 + 自定义 54 个）
