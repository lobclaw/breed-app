---
phase: 01-workbench-contract-test-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/home-workbench.ts
  - src/utils/homeWorkbench.ts
  - tests/utils/homeWorkbench.test.ts
autonomous: true
requirements:
  - WB-01
  - WB-02
  - WB-03
  - WB-04
  - WB-05
  - VERIFY-01
must_haves:
  truths:
    - "Developer can import buildHomeWorkbench and get exactly four semantic sections in order: overdue, breeding, reminders, therapy."
    - "Workbench rows preserve all source work while exposing visibleRows and hiddenCount for explicit fold UI later."
    - "Breeding milestone grouping is visual only: each workflow row carries one taskId and no batch-completion metadata."
    - "Health batch cards remain card rows with original tasks, dogs, and progress preserved."
    - "Medication dog rows sort pending before partial before done based on medication task facts."
    - "Adapter is pure: no cloud calls, no routing, no toast, no Vue/Pinia dependency, no suppression or red-dot state."
  artifacts:
    - path: "src/types/home-workbench.ts"
      provides: "Workbench view-model, card/task facades, and typed event payloads"
      exports:
        - "WORKBENCH_SECTION_ORDER"
        - "HomeWorkbenchModel"
        - "HomeWorkbenchEvent"
    - path: "src/utils/homeWorkbench.ts"
      provides: "Pure cards-to-workbench adapter"
      exports:
        - "buildHomeWorkbench"
    - path: "tests/utils/homeWorkbench.test.ts"
      provides: "Adapter behavior and purity tests"
      contains: "buildHomeWorkbench"
  key_links:
    - from: "tests/utils/homeWorkbench.test.ts"
      to: "src/utils/homeWorkbench.ts"
      via: "import buildHomeWorkbench"
      pattern: "buildHomeWorkbench"
    - from: "src/utils/homeWorkbench.ts"
      to: "src/types/home-workbench.ts"
      via: "type-only imports"
      pattern: "from '@/types/home-workbench'"
---

<objective>
Create the Phase 1 workbench contract: a typed view model, a pure adapter from already-filtered home cards, and tests that lock section order, grouping, hidden counts, medication ordering, and input immutability.

Purpose: Later phases can render dense workbench sections without touching cloud calls, route handlers, toast feedback, suppression, optimistic removal, or WeekStrip red-dot logic.
Output: `src/types/home-workbench.ts`, `src/utils/homeWorkbench.ts`, and `tests/utils/homeWorkbench.test.ts`.
</objective>

<execution_context>
@/Users/mooling/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mooling/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-workbench-contract-test-foundation/01-CONTEXT.md
@.planning/phases/01-workbench-contract-test-foundation/01-RESEARCH.md
@.planning/phases/01-workbench-contract-test-foundation/01-VALIDATION.md
@AGENTS.md
@graphify-out/GRAPH_REPORT.md
@src/components/smart-card/SmartCard.vue
@src/components/smart-card/DogCard.vue
@src/components/smart-card/BatchCard.vue
@src/components/smart-card/MedicationCard.vue
@src/components/smart-card/SickObservationCard.vue
@src/pages/home/index.vue
@vitest.config.ts

<interfaces>
From `src/components/smart-card/SmartCard.vue`:
```typescript
export interface SmartCardData {
  cardType: 'dog' | 'care_group' | 'batch' | 'medication' | 'health_attention' | 'sick_observation'
  domain?: 'breeding' | 'health' | 'medication'
  id: string
  priority: 'overdue' | 'today' | 'upcoming'
  overdueDays?: number
  tasks: any[]
  dogName?: string
  dogId?: string
  statusLabel?: string
  groupTitle?: string
  dogs?: any[]
  progress?: { done: number; total: number }
  [key: string]: any
}
```

Existing SmartCard emits that the new `HomeWorkbenchEvent` type must mirror:
```typescript
(e: 'complete', taskId: string, mode?: boolean | string): void
(e: 'postpone', taskIds: string | string[], title?: string): void
(e: 'batch-complete', payload: { taskIds: string[]; autoRecord?: boolean } | string[]): void
(e: 'batch-skip', taskIds: string[]): void
(e: 'batch-complete-med', medicationTaskIds: string[]): void
(e: 'action', payload: { type: string; data: any }): void
(e: 'record-dose', payload: { medicationTaskId: string }): void
```

From `src/pages/home/index.vue`, existing section metadata to preserve:
```typescript
overdue:  { title: '逾期待处理', dotColor: 'var(--red)' }
breeding: { title: '繁育流程', dotColor: 'var(--amber)' }
reminders:{ title: '健康提醒', dotColor: 'var(--blue)' }
therapy:  { title: '今日用药', dotColor: 'var(--plum)' }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define workbench model and typed event contract</name>
  <files>src/types/home-workbench.ts</files>
  <read_first>
    - src/types/home-workbench.ts (new file to create)
    - src/components/smart-card/SmartCard.vue (source of current card shape and emitted events)
    - src/pages/home/index.vue (source of section titles, dot colors, and side-effect boundary)
    - .planning/phases/01-workbench-contract-test-foundation/01-CONTEXT.md (locked decisions WB-01 through WB-05)
  </read_first>
  <behavior>
    - Export `WORKBENCH_SECTION_ORDER` exactly as `['overdue', 'breeding', 'reminders', 'therapy'] as const`.
    - Export `WorkbenchSectionKey`, `WorkbenchGroupKind`, `WorkbenchRowKind`, `MedicationRowState`, `HomeTask`, `HomeCard`, `WorkbenchRow`, `WorkbenchGroup`, `WorkbenchSection`, `HomeWorkbenchModel`, `BuildHomeWorkbenchOptions`, and `HomeWorkbenchEvent`.
    - `WorkbenchSection` and `WorkbenchGroup` must both include `rows`, `visibleRows`, `hiddenCount`, `count`, and `sortRank`.
    - `HomeWorkbenchEvent` must mirror SmartCard event payloads but must not include cloud-call, route, toast, suppression, or WeekStrip APIs.
  </behavior>
  <action>
    Create `src/types/home-workbench.ts` using TypeScript 4.9-compatible syntax and named exports only.

    Required concrete unions:
    - `WorkbenchGroupKind = 'overdue-domain' | 'breeding-step' | 'breeding-extra' | 'health-batch' | 'health-single' | 'health-illness' | 'medication-state'`
    - `WorkbenchRowKind = 'card' | 'task' | 'medication-dog'`
    - `MedicationRowState = 'pending' | 'partial' | 'done'`

    Required event union:
    - `{ type: 'complete'; taskId: string; mode?: boolean | string }`
    - `{ type: 'postpone'; taskIds: string | string[]; title?: string }`
    - `{ type: 'batch-complete'; payload: { taskIds: string[]; autoRecord?: boolean } | string[] }`
    - `{ type: 'batch-skip'; taskIds: string[] }`
    - `{ type: 'batch-complete-med'; medicationTaskIds: string[] }`
    - `{ type: 'record-dose'; payload: { medicationTaskId: string } }`
    - `{ type: 'action'; payload: { type: string; data: unknown } }`

    Use permissive facades for backend card facts: `HomeCard` may include `tasks?: HomeTask[]`, `dogs?: HomeWorkbenchDog[]`, `progress?: { done: number; total: number } | null`, `sectionType?: string`, `domain?: string`, `cardType: string`, `priority?: string`, and index signatures. Do not import Vue, Pinia, UniApp globals, stores, or page code in this type file.
  </action>
  <verify>
    <automated>pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "WORKBENCH_SECTION_ORDER = \\['overdue', 'breeding', 'reminders', 'therapy'\\] as const" src/types/home-workbench.ts` returns a match.
    - `rg -n "export type HomeWorkbenchEvent|batch-complete-med|record-dose|payload: \\{ medicationTaskId: string \\}" src/types/home-workbench.ts` returns matches.
    - `rg -n "visibleRows|hiddenCount|sortRank|HomeWorkbenchModel|BuildHomeWorkbenchOptions" src/types/home-workbench.ts` returns matches.
    - `rg -n "from 'vue'|from 'pinia'|uniCloud|useCloudCall|suppressedTaskMap|dayCounts|navigateTo|showToast" src/types/home-workbench.ts` returns no matches.
  </acceptance_criteria>
  <done>The workbench contract compiles and exposes all model and event types required by WB-01, WB-02, WB-04, and WB-05.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pure cards-to-workbench adapter with behavior tests</name>
  <files>src/utils/homeWorkbench.ts, tests/utils/homeWorkbench.test.ts</files>
  <read_first>
    - src/utils/homeWorkbench.ts (new file to create)
    - tests/utils/homeWorkbench.test.ts (new file to create)
    - src/types/home-workbench.ts (contract from Task 1)
    - src/pages/home/index.vue (existing section filters and color metadata)
    - src/components/smart-card/DogCard.vue (current `visibleTasks.slice(0, 3)` risk to avoid in adapter)
    - src/components/smart-card/BatchCard.vue (current `visibleDogs.slice(0, 12)` risk and health batch semantics)
    - src/components/smart-card/MedicationCard.vue (current medication `pending/partial/done` dose semantics)
    - .planning/phases/01-workbench-contract-test-foundation/01-RESEARCH.md (recommended adapter fixtures and assertions)
  </read_first>
  <behavior>
    - `buildHomeWorkbench(cards, { visibleRowLimit: 2 })` returns `sectionOrder` equal to `WORKBENCH_SECTION_ORDER`.
    - Cards with `priority === 'overdue'` go into the `overdue` section even when their business domain is breeding, health, or medication.
    - Non-overdue `sectionType: 'workflow'` breeding milestone cards group by `tasks[0].details.step_type` using keys like `breeding-step:follicle_check`.
    - `follicle_check`, `pregnancy_check`, and `weaning_confirm` titles are `建议卵泡检查`, `建议孕检`, and `确认断奶`; unknown step types fall back to task title, then card groupTitle, then `繁育流程`.
    - `sectionType: 'workflow_extra'` or `type: 'breeding_extra_arrangement'` rows stay in breeding under `breeding-extra:${kind || 'other'}`.
    - Health `cardType: 'batch'` rows preserve original `sourceCard.tasks`, `sourceCard.dogs`, and `sourceCard.progress`.
    - Health subtype grouping distinguishes `vaccination:${vaccine_type}` and `deworming:${deworming_type}:${drug_name}`.
    - Therapy medication card dog rows sort by `pending`, then `partial`, then `done`.
    - Every source row remains in `rows`; `visibleRows` is limited by `visibleRowLimit`; `hiddenCount` reports the remainder.
    - JSON stringification of input cards before and after adapter execution is identical.
  </behavior>
  <action>
    First write `tests/utils/homeWorkbench.test.ts` with inline fixture factories named `makeBreedingMilestoneCard`, `makeBreedingExtraCard`, `makeHealthBatchCard`, `makeHealthSingleCard`, `makeMedicationCard`, and `makeOverdueCard`. Then implement `src/utils/homeWorkbench.ts` until the tests pass.

    Required exports from `src/utils/homeWorkbench.ts`:
    - `WORKBENCH_SECTION_META` with the exact titles and dot colors from existing `src/pages/home/index.vue`.
    - `buildHomeWorkbench(cards: readonly HomeCard[], options?: BuildHomeWorkbenchOptions): HomeWorkbenchModel`.

    Required implementation rules:
    - Default `visibleRowLimit` is `4`.
    - Use `WORKBENCH_SECTION_ORDER.map(...)` to build all four sections even when a section has zero rows.
    - Compute `section.count` from all rows, not `visibleRows.length`.
    - Compute `totalCount` as the sum of all section counts.
    - Do not deep-clone or modify business objects; preserve read-only references as `sourceCard`, `sourceTask`, and `sourceDog`.
    - Do not import anything except types/constants from `@/types/home-workbench`.
    - Do not create UI components, Pinia state, backend calls, route handling, toast handling, suppression logic, or red-dot logic in this task.
  </action>
  <verify>
    <automated>pnpm test tests/utils/homeWorkbench.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "describe\\('buildHomeWorkbench'|sectionOrder|breeding-step:follicle_check|breeding-extra:|health-batch:|deworming:internal:海乐妙|medication-state:pending|hiddenCount|不修改输入" tests/utils/homeWorkbench.test.ts` returns matches.
    - `rg -n "export function buildHomeWorkbench|WORKBENCH_SECTION_META|visibleRowLimit|visibleRows|hiddenCount|medication-state:pending|medication-state:partial|medication-state:done" src/utils/homeWorkbench.ts` returns matches.
    - `rg -n "slice\\(0, 12\\)|slice\\(0, 3\\)" src/utils/homeWorkbench.ts` returns no matches.
    - `pnpm test tests/utils/homeWorkbench.test.ts` exits 0.
  </acceptance_criteria>
  <done>The adapter generates deterministic section/group/row metadata and passes the VERIFY-01 behavior suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add static purity guardrails for adapter boundary</name>
  <files>tests/utils/homeWorkbench.test.ts, src/utils/homeWorkbench.ts</files>
  <read_first>
    - tests/utils/homeWorkbench.test.ts (add purity/static assertions)
    - src/utils/homeWorkbench.ts (file under guard)
    - src/pages/home/index.vue (side effects that must remain page-owned)
    - src/composables/useCloudCall.ts (cloud-call boundary that adapter must not import)
    - .planning/phases/01-workbench-contract-test-foundation/01-VALIDATION.md (purity validation dimensions)
  </read_first>
  <behavior>
    - A test reads `src/utils/homeWorkbench.ts` as text and rejects forbidden boundary strings.
    - Forbidden strings include `uniCloud`, `useCloudCall`, `from 'vue'`, `from 'pinia'`, `navigateTo`, `showToast`, `suppressedTaskMap`, `dayCounts`, `latestLoadToken`, and `weekCache`.
    - The adapter test suite and TypeScript check both pass.
  </behavior>
  <action>
    Extend `tests/utils/homeWorkbench.test.ts` with a test named `不引入首页副作用边界依赖`. Use Node `fs` and `path` modules inside the test file to read `src/utils/homeWorkbench.ts`. Assert that none of the forbidden strings are present.

    Keep the adapter pure. If the static test fails, remove the offending import or reference rather than weakening the test. Do not add exceptions for Vue, Pinia, UniApp globals, `useCloudCall`, route APIs, toast APIs, suppression state, latest-token state, WeekStrip state, or `dayCounts`.
  </action>
  <verify>
    <automated>pnpm type-check && pnpm test tests/utils/homeWorkbench.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "不引入首页副作用边界依赖|forbiddenStrings|suppressedTaskMap|latestLoadToken|dayCounts|useCloudCall|uniCloud" tests/utils/homeWorkbench.test.ts` returns matches.
    - `rg -n "uniCloud|useCloudCall|from 'vue'|from 'pinia'|navigateTo|showToast|suppressedTaskMap|dayCounts|latestLoadToken|weekCache" src/utils/homeWorkbench.ts` returns no matches.
    - `pnpm type-check && pnpm test tests/utils/homeWorkbench.test.ts` exits 0.
  </acceptance_criteria>
  <done>Workbench adapter purity is enforced by an automated test and remains inside the Phase 1 side-effect boundary.</done>
</task>

</tasks>

<verification>
Run the plan-level gate after all tasks:

```bash
pnpm type-check && pnpm test tests/utils/homeWorkbench.test.ts
```
</verification>

<success_criteria>
- `src/types/home-workbench.ts` exports the stable contract and typed event union.
- `src/utils/homeWorkbench.ts` exports a pure `buildHomeWorkbench` adapter.
- `tests/utils/homeWorkbench.test.ts` covers section order, overdue priority, breeding step grouping, extra arrangement separation, health batch preservation, health subtype grouping, medication state sorting, hidden counts, input immutability, and purity guardrails.
- No cloud call, route, toast, suppression, red-dot, Vue, or Pinia dependency is introduced into the adapter.
</success_criteria>

<output>
After completion, create `.planning/phases/01-workbench-contract-test-foundation/01-01-SUMMARY.md`.
</output>
