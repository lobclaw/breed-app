---
phase: 01-workbench-contract-test-foundation
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/pages/home/index.vue
  - src/components/smart-card/DogCard.vue
  - src/components/smart-card/BatchCard.vue
autonomous: true
requirements:
  - WB-01
  - WB-02
  - WB-03
  - WB-04
  - WB-05
  - VERIFY-01
must_haves:
  truths:
    - "Home page can compute today's workbench model from already-suppressed `cards.value` without replacing current rendering."
    - "Home page can compute selected future day's workbench model from already-suppressed `dayCards.value` without replacing current rendering."
    - "Homepage-visible DogCard task tags and BatchCard dog chips keep compact defaults but expose inline `还有 X 项/只，展开` and `收起` controls instead of silently hiding work."
    - "Cloud calls, route handlers, toast feedback, suppression state, optimistic removal, batch metadata sync, latest-token protection, and dayCounts red-dot correction remain in `src/pages/home/index.vue`."
  artifacts:
    - path: "src/pages/home/index.vue"
      provides: "Low-risk computed integration point for buildHomeWorkbench"
      contains: "todayWorkbench"
    - path: "src/components/smart-card/DogCard.vue"
      provides: "Explicit expand/collapse affordance for compact dog-card task tags"
      contains: "hiddenTaskCount"
    - path: "src/components/smart-card/BatchCard.vue"
      provides: "Explicit expand/collapse affordance for compact batch-card dog chips"
      contains: "hiddenDogCount"
  key_links:
    - from: "src/pages/home/index.vue"
      to: "src/utils/homeWorkbench.ts"
      via: "computed buildHomeWorkbench calls"
      pattern: "buildHomeWorkbench\\(cards.value"
    - from: "src/components/smart-card/DogCard.vue"
      to: "card.tasks"
      via: "visibleTasks plus hiddenTaskCount/tasksExpanded control"
      pattern: "hiddenTaskCount|tasksExpanded"
    - from: "src/components/smart-card/BatchCard.vue"
      to: "card.dogs"
      via: "visibleDogs plus hiddenDogCount/dogsExpanded control"
      pattern: "hiddenDogCount|dogsExpanded"
---

<objective>
Wire the pure workbench adapter into the home page as computed state and close the current homepage-visible silent cap gap in existing smart cards without replacing full section UI or moving side effects.

Purpose: Prove Phase 1's contract can be generated from the same filtered card sources that existing UI uses, while ensuring WB-04 is not deferred: compact card rendering must disclose hidden tasks/dogs through explicit expand/collapse controls. Preserve the current home page as the owner of cloud calls, navigation, feedback, suppression, optimistic reconciliation, and red-dot correction.
Output: `todayWorkbench` and `dayWorkbench` computed values in `src/pages/home/index.vue`, plus explicit inline expand/collapse affordances in `DogCard.vue` and `BatchCard.vue`.
</objective>

<execution_context>
@/Users/mooling/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mooling/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-workbench-contract-test-foundation/01-CONTEXT.md
@.planning/phases/01-workbench-contract-test-foundation/01-RESEARCH.md
@.planning/phases/01-workbench-contract-test-foundation/01-VALIDATION.md
@.planning/phases/01-workbench-contract-test-foundation/01-01-SUMMARY.md
@.planning/phases/01-workbench-contract-test-foundation/01-02-SUMMARY.md
@AGENTS.md
@src/pages/home/index.vue
@src/utils/homeWorkbench.ts
@src/types/home-workbench.ts
@src/components/smart-card/DogCard.vue
@src/components/smart-card/BatchCard.vue

<interfaces>
From Plan 01 expected adapter contract:
```typescript
export function buildHomeWorkbench(
  cards: readonly HomeCard[],
  options?: BuildHomeWorkbenchOptions
): HomeWorkbenchModel
```

Existing home page side-effect functions that must remain page-owned:
```typescript
async function loadTodayCards(loadToken = latestLoadToken)
async function loadWeekCache(loadToken = latestLoadToken)
async function loadDateCounts(loadToken = latestLoadToken)
async function loadAll()
function filterSuppressedCards(cardList: any[])
function syncCardMeta(card: any, remainingTasks: any[])
function removeCardLocally(taskId: string, forceRemoveCard = false, showSuccess = true)
function applyHomeFeedback(payload: any)
```

Existing silent local caps to replace with explicit inline expand/collapse behavior:
```typescript
// src/components/smart-card/DogCard.vue
const visibleTasks = computed(() => (props.card.tasks || []).slice(0, 3))

// src/components/smart-card/BatchCard.vue
const visibleDogs = computed(() => (props.card.dogs || []).slice(0, 12))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add computed workbench integration without rendering changes</name>
  <files>src/pages/home/index.vue</files>
  <read_first>
    - src/pages/home/index.vue (modify only imports and computed state; do not change template rendering)
    - src/utils/homeWorkbench.ts (adapter export from Plan 01)
    - src/types/home-workbench.ts (model types from Plan 01)
    - .planning/phases/01-workbench-contract-test-foundation/01-01-SUMMARY.md (confirm adapter names created by Plan 01)
    - AGENTS.md (latest token, suppression, local reconciliation, and WeekStrip red-dot invariants)
  </read_first>
  <action>
    In `src/pages/home/index.vue`, import `buildHomeWorkbench` from `@/utils/homeWorkbench`.

    Add two computed values near existing `daySections` / `todaySections` computed state:
    - `const todayWorkbench = computed(() => buildHomeWorkbench(cards.value, { visibleRowLimit: 4 }))`
    - `const dayWorkbench = computed(() => buildHomeWorkbench(dayCards.value, { visibleRowLimit: 4 }))`

    Do not replace the existing template loops over `todaySections` or `daySections` in this phase. Do not change `summaryPills`, `breedingGroups`, `dayBreedingGroups`, `loadTodayCards`, `loadWeekCache`, `loadDateCounts`, `loadAll`, `filterSuppressedCards`, `syncCardMeta`, `removeCardLocally`, `applyHomeFeedback`, event handlers, navigation handlers, toast calls, suppression maps, `latestLoadToken`, or `dayCounts` update rules.

    The computed values may remain unused by the template until Phase 2. Their purpose is to prove the adapter can generate the workbench model from already-suppressed page state.
  </action>
  <verify>
    <automated>pnpm type-check && pnpm test tests/utils/homeWorkbench.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "import \\{ buildHomeWorkbench \\} from '@/utils/homeWorkbench'" src/pages/home/index.vue` returns a match.
    - `rg -n "const todayWorkbench = computed\\(\\(\\) => buildHomeWorkbench\\(cards\\.value, \\{ visibleRowLimit: 4 \\}\\)\\)" src/pages/home/index.vue` returns a match.
    - `rg -n "const dayWorkbench = computed\\(\\(\\) => buildHomeWorkbench\\(dayCards\\.value, \\{ visibleRowLimit: 4 \\}\\)\\)" src/pages/home/index.vue` returns a match.
    - `rg -n "latestLoadToken|filterSuppressedCards|syncCardMeta|removeCardLocally|applyHomeFeedback|dayCounts\\.value\\[todayTs\\]" src/pages/home/index.vue` still returns matches.
    - `pnpm type-check && pnpm test tests/utils/homeWorkbench.test.ts` exits 0.
  </acceptance_criteria>
  <done>Home page computes workbench state from already-filtered card arrays while current rendering and side-effect ownership remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Replace visible smart-card silent caps with inline expand controls</name>
  <files>src/components/smart-card/DogCard.vue, src/components/smart-card/BatchCard.vue</files>
  <read_first>
    - src/components/smart-card/DogCard.vue (replace current `visibleTasks` silent `slice(0, 3)` with compact + expand state)
    - src/components/smart-card/BatchCard.vue (replace current `visibleDogs` silent `slice(0, 12)` with compact + expand state)
    - AGENTS.md (WB-04 homepage card non-truncation rule; no Sheet/no secondary page/no backend call)
    - .planning/phases/01-workbench-contract-test-foundation/01-CONTEXT.md (Phase 1 locked decisions and deferred full dense UI work)
  </read_first>
  <action>
    In `src/components/smart-card/DogCard.vue`, keep the default compact display of the first 3 task tags but make the hidden remainder explicit:
    - Add `const TASK_COMPACT_LIMIT = 3`.
    - Add `const tasksExpanded = ref(false)`.
    - Add `allTasks`, `hiddenTaskCount`, and `taskExpandText` computed values.
    - Change `visibleTasks` so it returns all tasks when `tasksExpanded.value` is true, otherwise the first `TASK_COMPACT_LIMIT` tasks.
    - Render an inline control immediately after the task tag list when `hiddenTaskCount > 0`. The visible text must be `还有 X 项，展开` while collapsed and `收起` while expanded.
    - The control must use `@click.stop` and only toggle `tasksExpanded`; it must not trigger card navigation, complete, postpone, skip, or any cloud/backend work.
    - Preserve the existing action buttons and their use of `visibleTasks[0]` for the primary task.

    In `src/components/smart-card/BatchCard.vue`, keep the default compact display of the first 12 dogs but make the hidden remainder explicit:
    - Add `const DOG_COMPACT_LIMIT = 12`.
    - Add `const dogsExpanded = ref(false)`.
    - Add `allDogs`, `hiddenDogCount`, and `dogExpandText` computed values.
    - Change `visibleDogs` so it returns all dogs when `dogsExpanded.value` is true, otherwise the first `DOG_COMPACT_LIMIT` dogs.
    - Render an inline control immediately after the checkbox list when `hiddenDogCount > 0`. The visible text must be `还有 X 只，展开` while collapsed and `收起` while expanded.
    - The control must use `@click.stop` and only toggle `dogsExpanded`; it must not open a Sheet, route to a secondary page, call backend APIs, or change checked dog state.

    Add small scoped styles for the inline expand controls in each component, reusing existing tokens (`var(--text-2)`, `var(--card-dim)`, current color classes if useful). Do not add BSheet, BottomSheet, new backend calls, new route handlers, or a full home section UI replacement. This task directly satisfies WB-04 for the current homepage-visible card path while keeping Phase 1 low-risk.
  </action>
  <verify>
    <automated>pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "TASK_COMPACT_LIMIT|tasksExpanded|hiddenTaskCount|taskExpandText|还有 .* 项，展开|收起" src/components/smart-card/DogCard.vue` returns matches.
    - `rg -n "DOG_COMPACT_LIMIT|dogsExpanded|hiddenDogCount|dogExpandText|还有 .* 只，展开|收起" src/components/smart-card/BatchCard.vue` returns matches.
    - `rg -n "visibleTasks = computed\\(\\(\\) => \\(props\\.card\\.tasks \\|\\| \\[\\]\\)\\.slice\\(0, 3\\)\\)|visibleDogs = computed\\(\\(\\) => \\(props\\.card\\.dogs \\|\\| \\[\\]\\)\\.slice\\(0, 12\\)\\)" src/components/smart-card/DogCard.vue src/components/smart-card/BatchCard.vue` returns no matches.
    - `rg -n "BSheet|BottomSheet|uniCloud|useCloudCall|showActionSheet" src/components/smart-card/DogCard.vue src/components/smart-card/BatchCard.vue` returns no matches.
    - `pnpm type-check` exits 0.
  </acceptance_criteria>
  <done>DogCard and BatchCard no longer silently hide additional homepage-visible tasks/dogs; users can explicitly expand and collapse the compact rows without leaving the card path.</done>
</task>

<task type="auto">
  <name>Task 3: Run Phase 1 integrated validation and graph refresh</name>
  <files>src/pages/home/index.vue, src/components/smart-card/DogCard.vue, src/components/smart-card/BatchCard.vue</files>
  <read_first>
    - src/pages/home/index.vue (confirm integration remains computed-only)
    - src/components/smart-card/DogCard.vue (confirm explicit task expand/collapse remains local UI state only)
    - src/components/smart-card/BatchCard.vue (confirm explicit dog expand/collapse remains local UI state only)
    - .planning/phases/01-workbench-contract-test-foundation/01-01-SUMMARY.md (adapter implementation summary)
    - .planning/phases/01-workbench-contract-test-foundation/01-02-SUMMARY.md (task-service test additions consumed by this integrated validation)
    - .planning/phases/01-workbench-contract-test-foundation/01-VALIDATION.md (phase validation commands)
    - AGENTS.md (graphify rebuild requirement after code changes)
    - vitest.config.ts (test include and coverage scope)
  </read_first>
  <action>
    Run the Phase 1 validation commands in this order:
    1. `pnpm type-check`
    2. `pnpm test tests/utils/homeWorkbench.test.ts tests/cloud-objects/task-service.test.ts`
    3. `pnpm test`
    4. `./scripts/graphify-rebuild.sh`

    Before graphify rebuild, run the WB-04 static checks from Task 2 acceptance criteria so the summary can state that explicit expand/collapse controls replaced the current silent caps.

    If a command fails because of the changes in this phase, fix the relevant file and rerun the failed command. If `./scripts/graphify-rebuild.sh` changes graph artifacts, leave those generated changes in place and mention them in the summary. Do not edit code to bypass tests.
  </action>
  <verify>
    <automated>pnpm type-check && pnpm test tests/utils/homeWorkbench.test.ts tests/cloud-objects/task-service.test.ts && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm type-check` exits 0.
    - `pnpm test tests/utils/homeWorkbench.test.ts tests/cloud-objects/task-service.test.ts` exits 0.
    - `pnpm test` exits 0.
    - `rg -n "TASK_COMPACT_LIMIT|tasksExpanded|hiddenTaskCount|taskExpandText|DOG_COMPACT_LIMIT|dogsExpanded|hiddenDogCount|dogExpandText" src/components/smart-card/DogCard.vue src/components/smart-card/BatchCard.vue` returns matches.
    - `./scripts/graphify-rebuild.sh` exits 0 after code changes.
  </acceptance_criteria>
  <done>Phase 1 has a green type/test gate, WB-04 is covered in the visible smart-card path, and graphify has been refreshed after code changes.</done>
</task>

</tasks>

<verification>
Run the final Phase 1 gate:

```bash
pnpm type-check && pnpm test tests/utils/homeWorkbench.test.ts tests/cloud-objects/task-service.test.ts && pnpm test
```

Then run:

```bash
./scripts/graphify-rebuild.sh
```

Also verify the WB-04 smart-card controls:

```bash
rg -n "TASK_COMPACT_LIMIT|tasksExpanded|hiddenTaskCount|taskExpandText" src/components/smart-card/DogCard.vue
rg -n "DOG_COMPACT_LIMIT|dogsExpanded|hiddenDogCount|dogExpandText" src/components/smart-card/BatchCard.vue
rg -n "visibleTasks = computed\\(\\(\\) => \\(props\\.card\\.tasks \\|\\| \\[\\]\\)\\.slice\\(0, 3\\)\\)|visibleDogs = computed\\(\\(\\) => \\(props\\.card\\.dogs \\|\\| \\[\\]\\)\\.slice\\(0, 12\\)\\)" src/components/smart-card/DogCard.vue src/components/smart-card/BatchCard.vue
```

The final `rg` command must return no matches.
</verification>

<success_criteria>
- `src/pages/home/index.vue` imports and computes `buildHomeWorkbench` from already-suppressed `cards.value` and `dayCards.value`.
- Existing section UI rendering remains unchanged for Phase 1; only DogCard/BatchCard local compact rows gain explicit expand/collapse controls.
- `DogCard.vue` shows `还有 X 项，展开` / `收起` for task tags beyond the compact limit and can reveal all tasks without navigation or backend work.
- `BatchCard.vue` shows `还有 X 只，展开` / `收起` for dogs beyond the compact limit and can reveal all dogs without Sheet, secondary page, or backend work.
- Existing home page side-effect functions remain present and page-owned.
- Adapter tests, task-service tests, full Vitest suite, type check, and graphify rebuild all pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-workbench-contract-test-foundation/01-03-SUMMARY.md`.
</output>
