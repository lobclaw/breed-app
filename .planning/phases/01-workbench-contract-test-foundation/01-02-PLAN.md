---
phase: 01-workbench-contract-test-foundation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/cloud-objects/task-service.test.ts
autonomous: true
requirements:
  - WB-04
  - VERIFY-02
must_haves:
  truths:
    - "Task-service tests prove deworming subtype keys include both deworming_type and drug_name."
    - "Task-service tests prove future medication-only dates return therapy cards, not only red-dot counts."
    - "Task-service tests prove weaning_confirm breeding milestones remain individual dog/litter workflow cards and never batch cards."
  artifacts:
    - path: "tests/cloud-objects/task-service.test.ts"
      provides: "Cloud-object invariant coverage for Phase 1 backend facts"
      contains: "getWeekCards"
  key_links:
    - from: "tests/cloud-objects/task-service.test.ts"
      to: "uniCloud-alipay/cloudfunctions/task-service/index.obj.js"
      via: "taskService._mergeTasks and taskService.getWeekCards"
      pattern: "_mergeTasks|getWeekCards"
---

<objective>
Strengthen `task-service` invariant tests for the backend facts the workbench adapter consumes.

Purpose: Later UI density work can trust that backend cards are untruncated, health subtype identities do not collide, future medication-only dates have actual therapy cards, and breeding workflow items do not become batch cards.
Output: Additional tests in `tests/cloud-objects/task-service.test.ts`.
</objective>

<execution_context>
@/Users/mooling/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mooling/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-workbench-contract-test-foundation/01-CONTEXT.md
@.planning/phases/01-workbench-contract-test-foundation/01-RESEARCH.md
@.planning/phases/01-workbench-contract-test-foundation/01-VALIDATION.md
@AGENTS.md
@tests/cloud-objects/task-service.test.ts
@tests/helpers/mock-unicloud.ts
@uniCloud-alipay/cloudfunctions/task-service/index.obj.js

<interfaces>
From `uniCloud-alipay/cloudfunctions/task-service/index.obj.js`:
```javascript
function getTaskVariantKey(task) {
  if (task.type === 'vaccination') return `vaccination:${task.details?.vaccine_type || ''}`
  if (task.type === 'deworming') return `deworming:${task.details?.deworming_type || ''}:${task.details?.drug_name || ''}`
}
```

Existing test setup from `tests/cloud-objects/task-service.test.ts`:
```typescript
const mockUniCloud = createMockUniCloud()
;(globalThis as any).uniCloud = mockUniCloud
process.env.NODE_ENV = 'test'
const taskService = require('../../uniCloud-alipay/cloudfunctions/task-service/index.obj.js')
const mergeTasks: (...args: any[]) => any = taskService._mergeTasks
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deworming subtype collision test</name>
  <files>tests/cloud-objects/task-service.test.ts</files>
  <read_first>
    - tests/cloud-objects/task-service.test.ts (add test under `describe('mergeTasks 健康卡片路由')`)
    - uniCloud-alipay/cloudfunctions/task-service/index.obj.js (read `getTaskVariantKey` and `mergeTasks`)
    - .planning/phases/01-workbench-contract-test-foundation/01-RESEARCH.md (VERIFY-02 required additions)
  </read_first>
  <behavior>
    - Two same-day internal deworming tasks with `drug_name: '海乐妙'` merge into one batch card.
    - Two same-day external deworming tasks with `drug_name: '福来恩'` merge into a separate batch card.
    - The two batch card IDs are distinct and contain `deworming:internal:海乐妙` and `deworming:external:福来恩`.
  </behavior>
  <action>
    In `tests/cloud-objects/task-service.test.ts`, add a test named `不同驱虫类型和药名的同日任务不应合并到同一批量卡` inside the existing `describe('mergeTasks 健康卡片路由')` block.

    Use four pending task fixtures with the same `due_date`:
    - `dew_internal_1`, `dew_internal_2`: `type: 'deworming'`, `details: { deworming_type: 'internal', drug_name: '海乐妙' }`
    - `dew_external_1`, `dew_external_2`: `type: 'deworming'`, `details: { deworming_type: 'external', drug_name: '福来恩' }`

    Call `const cards = mergeTasks(tasks, [], [])`. Assert:
    - `cards` has length `2`.
    - every card has `cardType === 'batch'`.
    - one card id includes `batch-deworming:internal:海乐妙-`.
    - one card id includes `batch-deworming:external:福来恩-`.
    - `new Set(cards.map((card: any) => card.id)).size` is `2`.
  </action>
  <verify>
    <automated>pnpm test tests/cloud-objects/task-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "不同驱虫类型和药名的同日任务不应合并到同一批量卡|dew_internal_1|dew_external_1|deworming:internal:海乐妙|deworming:external:福来恩" tests/cloud-objects/task-service.test.ts` returns matches.
    - `pnpm test tests/cloud-objects/task-service.test.ts` exits 0.
  </acceptance_criteria>
  <done>Deworming subtype collision is guarded by a cloud-object test, satisfying the health subtype portion of VERIFY-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add future medication card and weaning non-batch tests</name>
  <files>tests/cloud-objects/task-service.test.ts</files>
  <read_first>
    - tests/cloud-objects/task-service.test.ts (add tests under existing `getDateCounts / getHomeCards 当前首页规则` and `mergeTasks 健康卡片路由` blocks)
    - uniCloud-alipay/cloudfunctions/task-service/index.obj.js (read `computeMedItemsForDay`, `getWeekCards`, and breeding standalone card generation)
    - tests/helpers/mock-unicloud.ts (confirm seedCollection and createCloudObjectContext patterns)
    - AGENTS.md (WeekStrip future medication red-dot and card consistency rule)
  </read_first>
  <behavior>
    - A future date with only an active `medication_tasks` record returns `sections.therapy` with one medication card from `getWeekCards`.
    - A `breeding_milestone` task with `details.step_type: 'weaning_confirm'` and a `litter_id` returns a `dog` workflow card, not a `batch` card.
  </behavior>
  <action>
    Add two tests.

    First, under `describe('getDateCounts / getHomeCards 当前首页规则')`, add `未来日期仅有疗程状态时 getWeekCards 也应返回今日用药卡`. Seed:
    - one `medication_tasks` document with `_id: 'med_future_week_1'`, `family_id`, `dog_id: 'dog_1'`, `dog_name: '肉肉'`, `status: '进行中'`, `actual_start_date: todayStart.getTime()`, `frequency: 1`, `duration_days: 3`, and `daily_doses: {}`.
    - empty `tasks` and `health_records`.
    Call `taskService.getWeekCards.call(ctx, tomorrow, tomorrow + DAY_MS - 1)`. Assert `res.data[tomorrow].sections.therapy` has length `1`, the card has `cardType: 'medication'`, `domain: 'medication'`, and `dogs[0].dogId === 'dog_1'`.

    Second, under `describe('mergeTasks 健康卡片路由')`, add `确认断奶繁育流程节点带 litter_id 也不应合并为批量卡`. Build two same-day pending `breeding_milestone` tasks with `litter_id: 'litter_1'`, different `_id` and `dog_id`, `details: { step_type: 'weaning_confirm' }`, and call `mergeTasks(tasks, [], [])`. Assert every returned card has `cardType === 'dog'`, `sectionType` is not set by mergeTasks at this layer, `domain === 'breeding'`, and no returned card has `cardType === 'batch'`.
  </action>
  <verify>
    <automated>pnpm test tests/cloud-objects/task-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "未来日期仅有疗程状态时 getWeekCards 也应返回今日用药卡|med_future_week_1|sections\\.therapy|cardType\\)\\.toBe\\('medication'\\)" tests/cloud-objects/task-service.test.ts` returns matches.
    - `rg -n "确认断奶繁育流程节点带 litter_id 也不应合并为批量卡|weaning_confirm|litter_1|not\\.toBe\\('batch'\\)" tests/cloud-objects/task-service.test.ts` returns matches.
    - `pnpm test tests/cloud-objects/task-service.test.ts` exits 0.
  </acceptance_criteria>
  <done>Future medication card presence and breeding weaning non-batch behavior are guarded by task-service tests, satisfying the remaining VERIFY-02 additions.</done>
</task>

</tasks>

<verification>
Run the plan-level gate after both tasks:

```bash
pnpm test tests/cloud-objects/task-service.test.ts
```
</verification>

<success_criteria>
- Deworming subtype grouping is protected by exact `deworming_type + drug_name` tests.
- Future medication-only days are proven to return actual WeekStrip day cards in `sections.therapy`, not only counts.
- `weaning_confirm` breeding milestones with `litter_id` remain non-batch workflow facts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-workbench-contract-test-foundation/01-02-SUMMARY.md`.
</output>
