{
  "bsonType": "object",
  "required": ["dog_id", "family_id", "weight", "date"],
  "permission": {
    "read": true,
    "create": true,
    "update": true,
    "delete": true
  },
  "properties": {
    "_id": {
      "description": "体重记录ID"
    },
    "dog_id": {
      "bsonType": "string",
      "foreignKey": "dogs._id",
      "description": "关联犬只ID"
    },
    "family_id": {
      "bsonType": "string",
      "foreignKey": "families._id",
      "description": "所属家庭ID"
    },
    "weight": {
      "bsonType": "number",
      "description": "体重（kg）"
    },
    "date": {
      "bsonType": "number",
      "description": "测量日期（时间戳毫秒）"
    },
    "version": {
      "bsonType": "number",
      "description": "同步版本号，每次服务端成功写入递增"
    },
    "created_at": {
      "bsonType": "number",
      "description": "创建时间（时间戳毫秒）"
    },
    "updated_at": {
      "bsonType": "number",
      "description": "更新时间（时间戳毫秒）"
    }
  }
}
